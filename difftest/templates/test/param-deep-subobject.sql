--@userId int user.userId
--@userName varchar user.name
--@petType varchar user.pet.type
--@petName varchar user.pet.detail.name 

select @userId as userId, @userName as name, @petType as petType, @petName as petName;