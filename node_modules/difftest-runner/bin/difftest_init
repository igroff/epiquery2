#! /usr/bin/env bash
# vi: ft=sh

###
### difftest default
###
### this is the help for difftest
###

MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ${MY_DIR}/utils

mkdir -p "${TEST_ROOT}"
mkdir -p "${RESULTS_DIR}"
mkdir -p "${EXPECTED_DIR}"
mkdir -p "${TEST_BIN_DIR}"

echo "difftest initialized, in dir ${TEST_ROOT}"

