#! /usr/bin/env bash
# vi: ft=sh

###
### difftest default
###
### this is the help for difftest
###

MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ${MY_DIR}/utils

show_help ${BASH_SOURCE[0]}
